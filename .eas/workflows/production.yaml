name: workflow for production

on:
  push:
    tags:
      - 'v*-production'
 
jobs:
  build_production:
    name: building crossword production
    type: build
    params:
      platform: android
      profile: production 